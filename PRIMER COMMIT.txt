Usuario busca "Faker#KR1"
        │
        ▼
Django recibe la petición
        │
        ▼
¿Ya tengo a este invocador en Postgres
y se actualizó hace menos de X minutos?
        │
   ┌────┴────┐
  SÍ          NO
   │           │
   │           ▼
   │    Llamar a Riot API:
   │    1. ACCOUNT-V1 → obtener PUUID
   │    2. SUMMONER-V4 → nivel, ícono
   │    3. LEAGUE-V4 → rank/liga
   │    4. MATCH-V5 → lista de partidas + detalle
   │           │
   │           ▼
   │    Guardar/actualizar en Postgres
   │    (Invocador, Liga, Partida, Participante)
   │           │
   └─────┬─────┘
         ▼
Django calcula winrate, KDA, stats por campeón
a partir de lo que hay en Postgres
         │
         ▼
Devuelve JSON a React
         │
         ▼
React renderiza la página de perfil